@{
    var navigationKey = ViewBag.ManageNavigationKey;
}
<div class="header-nav">
    <div class="hidden-xs">
        <ul class="nav nav-tabs">
            <li class="@(navigationKey == "privacypolicy" ? "active" : null)"><a href="@Url.PrivacyPolicy()">Privacy policy</a></li>
            <li class="@(navigationKey == "useragreement" ? "active" : null)"><a href="@Url.UserAgreement()">User agreement</a></li>
            <li class="@(navigationKey == "contentpolicy" ? "active" : null)"><a href="@Url.ContentPolicy()">Content policy</a></li>
        </ul>
    </div>
    <div class="visible-xs-block">
        <ul class="nav nav-tabs">
            <li class="dropdown active">
                <a class="dropdown-toggle" data-toggle="dropdown" href="javascript:void(0);">
                    @ViewBag.Title <span class="caret"></span>
                </a>
                <ul class="dropdown-menu">
                    <li class="@(navigationKey == "privacypolicy" ? "active" : null)"><a href="@Url.PrivacyPolicy()">Privacy policy</a></li>
                    <li class="@(navigationKey == "useragreement" ? "active" : null)"><a href="@Url.UserAgreement()">User agreement</a></li>
                    <li class="@(navigationKey == "contentpolicy" ? "active" : null)"><a href="@Url.ContentPolicy()">Content policy</a></li>
                </ul>
            </li>
        </ul>
    </div>
</div>
